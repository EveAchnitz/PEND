let altura;

console.log(altura);

//O resultado exibido no console é:undefined.
//Isso aconteceu porque não atribuímos nenhum valor para a variável, nós apenas a declaramos e não inicializamos. Logo, não há nada para exibir nela.